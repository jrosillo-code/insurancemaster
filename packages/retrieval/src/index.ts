/**
 * @rosillo/retrieval — authorised search over structured records and approved documents.
 *
 * Structured facts before semantic similarity (ADR-0004); everything filtered by the
 * caller's scope and by effective dates; conflicts surfaced rather than resolved.
 */

export * from './plan';
export * from './retrieve';
